import React from "react";

const DeathMode = () => {
  return (
    <div className="DeathMode">
      <label className="mode">
        <input type="checkbox" />
        <span className="slider round"></span>
      </label>
    </div>
  );
};

export default DeathMode;
