import Logo from "./Logo";
import Search from "./Search";
import UserInfo from "./UserInfo";

const Header = () => {
  return (
    <header>
      <Logo />
      <Search />
      <UserInfo />
    </header>
  );
};

export default Header;
